[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    live: network-dependent integration tests (excluded from default gates)
addopts = -m "not live"
