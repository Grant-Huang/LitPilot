---
description: LitPilot 流式会话 UI 设计标准（工作流时间线 + 思考区）
globs: frontend/src/components/chat/**/*.{tsx,css},frontend/src/lib/turnWorkflow.ts,frontend/src/lib/turnCompletion.ts,frontend/src/styles/meso-platform.css
alwaysApply: false
---

# LitPilot 流式会话 UI

适用于 `LitPilotLiveTurn`、`TurnWorkflowBlock`、`WorkflowCardView`、`LitPilotThinkFold` 及 `turnWorkflow` / `turnCompletion` 数据层。

## 信息架构

- **进度文案只出现一处**：动态 `literature_progress.detail` 仅服务于当前 running 卡片内的思考流；不得同时写入顶部 headline、已完成卡片摘要、Brief 伪卡片 body。
- **输入器芯片**：`literature_progress` 的 `parallel` / `in_flight` / `elapsed_sec` 亦驱动输入器区域（并行度芯片、静默计时提示），此属输入器独立职责，不与卡片内思考流重复。
- **流式 headline**：`TurnCompletionBar` 仅在回合完成后显示统计摘要，streaming 时不渲染。
- **Brief 卡片**：仅在有 `literature_brief_assessment` 结构化结果（RQ / 关键词 / hint）时创建；禁止用心跳 progress 文案伪造「Brief 评估 · 已完成」。

## 思考区（LitPilotThinkFold）

- **流式 flatten**：`litpilot-think--streaming` 时无 border/背景，仅左侧 2px 边线，避免 workflow 卡片内「盒中盒」。
- **可读性**：流式正文 14px / 1.6 行高 / 次要文本色（灰一点）；系统注记（⟦sys⟧）保持 11–12px 灰色。
- **滚动**：流式区 `max-height: 280px`，内容增长时自动滚到底；容器加 `aria-live="polite"`。
- **标签**：streaming 时 header 文案为「思考中」，完成后为「推理过程」。
- **折叠**：步骤进行中默认展开，允许用户手动收起；步骤完成或 streaming 结束自动折叠。

## 进度指示

- **单一强指示**：有思考流时，仅保留思考区 header dot + 正文光标 `▋`；去掉卡片标题旁 spinner 与「进行中」pill。
- **无思考流时**：卡片 head 保留 spinner 或「进行中」，二者不同时出现。

## 反例（避免）

- 同一句 progress detail 在顶部、Brief 摘要、log 行各出现一次
- 推理区独立卡片样式嵌在 workflow 卡片 body 内
- streaming 时 headline 显示 transient progress 而非阶段名
- 步骤已完成仍强制展开推理区
