# UI 路由

| 路径 | 说明 |
|------|------|
| `/` | → `/chat` |
| `/chat` | 主会话 + 右侧 Artifact |
| `/library` | 文献库（ref-list + PDF） |
| `/settings` | Tavily / Jina / LLM 设置 |

Artifact 展开状态保存在 `localStorage`：`litpilot:artifact-visible`。
